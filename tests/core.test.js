import test from 'node:test';import assert from 'node:assert/strict';
import {makePlan,validPlan,mergePlan,secondsLeft} from '../app/src/main/assets/core.js';
import {normalizeExercise,resizeSets,setSetValue,estimatePlanMinutes,fitPlanDuration,summarizeHistory} from '../app/src/main/assets/training.js';
import {readFileSync} from 'node:fs';
const catalog=JSON.parse(readFileSync(new URL('../app/src/main/assets/catalog.json',import.meta.url)));
const ids=catalog.map(e=>e.id);
test('all starter plans use real catalog entries and per-set arrays',()=>{for(const day of ['Push','Pull','Legs']){const p=makePlan(day);assert.ok(validPlan(p,ids));assert.equal(p.exercises[0].setReps.length,p.exercises[0].sets);}});
test('reject unsafe and malformed plans',()=>{for(const [key,value] of [['sets',0],['sets',11],['reps',51],['rest',-1],['weight',Infinity],['id','invented']]){const p=makePlan();p.exercises[0][key]=value;assert.equal(validPlan(p,ids),false);}const p=makePlan();p.exercises.push(p.exercises[0]);assert.equal(validPlan(p,ids),false);assert.equal(validPlan({name:'empty',exercises:[]},ids),false);const q=makePlan();q.exercises[0].setReps=[12];assert.equal(validPlan(q,ids),false);});
test('sets keep independent reps and weights and resize without flattening',()=>{let e=normalizeExercise({id:'Pushups',sets:3,reps:12,rest:90,weight:0});e=setSetValue(e,1,'reps',9);e=setSetValue(e,2,'reps',7);e=setSetValue(e,1,'weight',5);assert.deepEqual(e.setReps,[12,9,7]);assert.deepEqual(e.setWeights,[0,5,0]);e=resizeSets(e,4);assert.deepEqual(e.setReps,[12,9,7,7]);});
test('AI updates preserve completed set performance and clamp reduced set count',()=>{const old=makePlan();old.exercises[0].setReps=[12,10,8];old.exercises[0].done=3;const next=makePlan();next.exercises[0]=resizeSets(next.exercises[0],2);next.exercises[0].setReps=[15,15];const merged=mergePlan(old,next);assert.equal(merged.exercises[0].done,2);assert.deepEqual(merged.exercises[0].setReps,[12,10]);assert.equal(old.exercises[0].done,3);});
test('duration fitter expands short bodyweight sessions toward the requested training time without a +5 ceiling',()=>{const plan={name:'Bodyweight',exercises:['Pushups','Bodyweight_Squat'].map(id=>normalizeExercise({id,sets:3,reps:12,rest:75,weight:0}))};const fit=fitPlanDuration(plan,60,catalog);assert.ok(fit.estimatedMinutes>=60);assert.ok(fit.plan.exercises.length>=2&&fit.plan.exercises.length<=12);assert.ok(fit.plan.exercises.every(e=>catalog.find(c=>c.id===e.id).equipment==='body only'));});
test('history summary retains each completed set separately',()=>{const p=makePlan();p.exercises[0].setReps=[12,9,7];p.exercises[0].done=2;const summary=summarizeHistory([{date:'2026-09-20',day:'mon',plan:p}],catalog);assert.deepEqual(summary[0].exercises[0].sets.map(s=>s.reps),[12,9]);});
test('estimated duration excludes warm-up and grows with volume',()=>{const one={name:'One',exercises:[normalizeExercise({id:'Pushups',sets:1,reps:10,rest:90,weight:0})]};assert.ok(estimatePlanMinutes(one)<5);const p=makePlan();const first=estimatePlanMinutes(p);p.exercises=p.exercises.map(e=>resizeSets(e,5));assert.ok(estimatePlanMinutes(p)>first);});
test('duration fitter does not trim a valid workout just because it runs over the target',()=>{const p=makePlan();p.exercises=p.exercises.map(e=>resizeSets(e,6));const before=estimatePlanMinutes(p);const fit=fitPlanDuration(p,20,catalog);assert.equal(fit.estimatedMinutes,before);assert.equal(fit.plan.exercises.length,p.exercises.length);});
test('rest timer uses wall clock, survives background delay and pause',()=>{assert.equal(secondsLeft({end:10000},1000),9);assert.equal(secondsLeft({end:10000},15000),0);assert.equal(secondsLeft({paused:12},999999),12);});
